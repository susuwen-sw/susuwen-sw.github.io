<div class="experience">
  {% if site.experience != blank %}
    {% assign experience_size = site.experience | size %}
    <div
      class="table-responsive"
      {% if include.limit and site.announcements.scrollable and experience_size > 3 %}
        style="max-height: 60vw"
      {% endif %}
    >
      <table class="table table-sm table-borderless">
        {% assign experience = site.experience | reverse %}
        {% if include.limit and site.announcements.limit %}
          {% assign experience_limit = site.announcements.limit %}
        {% else %}
          {% assign experience_limit = experience_size %}
        {% endif %}
        {% for item in experience limit: experience_limit %}
          <tr>
            <th scope="row" style="width: 20%">{{ item.date | date: '%m, %Y' }}</th>
            <td>
              {% if item.inline %}
                {{ item.content | remove: '<p>' | remove: '</p>' | emojify }}
              {% else %}
                <a class="experience-title" href="{{ item.url | relative_url }}">{{ item.title }}</a>
              {% endif %}
            </td>
          </tr>
        {% endfor %}
      </table>
    </div>
  {% else %}
    <p>No experience History so far...</p>
  {% endif %}
</div>
